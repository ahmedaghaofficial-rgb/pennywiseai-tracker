<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Sideload-safe personal debug build.
      Google Play Protect can block APKs installed from browsers/file managers
      when they request SMS or notification-listener access. Keep those
      capabilities in personalRelease; remove them only from personalDebug so
      the owner can install and test the rest of the product safely.
    -->
    <uses-permission
        android:name="android.permission.READ_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_SMS"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.READ_CONTACTS"
        tools:node="remove" />

    <application>
        <receiver
            android:name="com.pennywiseai.tracker.receiver.SmsBroadcastReceiver"
            tools:node="remove" />

        <service
            android:name="com.pennywiseai.tracker.receiver.BankNotificationListenerService"
            tools:node="remove" />
    </application>
</manifest>
